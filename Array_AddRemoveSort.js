// Create an array named studentNames with the names of your students
let studentNames = ["Alice", "Bob", "Charlie", "David", "Eve"];

// 1) Add a new student name to the beginning of the array
studentNames.unshift("Frank");

// 2) Remove the last student name from the array
studentNames.pop();

console.log(studentNames);

// 3) Alphabetize the student names within the array
studentNames.sort();

// Output the final array
console.log(studentNames);
